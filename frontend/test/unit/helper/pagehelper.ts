//import Vue = vuejs.Vue;
/**
 * ページ構築したりvueのコンポーネントを用意したりするヘルパー群
 */
class PageHalper {

    /**
     * ページをロードしてルーティング等が行われるようにする
     */
    static appStart() : void {
        return;
    }

    /**
     *
     */
    // static componentInit(componentName: string) : Vue {
    //     return new Vue({
    //
    //     }).$mount();
    // }
}
